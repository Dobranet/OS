#!/bin/bash
function help () {
echo "calc key has action sum/sub/mul/div,You need to enter 2 arguments"
echo "search key must get the argument as a string to be found"
echo "reverse key, as arguments you must write the name of the file to be written in reverse order "
echo "and the file to write the result to"
echo "strlen key is to get a string as an input"
echo "log key displays warnings and informational messages"
echo "exit key terminates the script and outputs an error code or 0 as a result of successful"
echo " completion"
echo "interactive key puts the program into interactive use"
}
